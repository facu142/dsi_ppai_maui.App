<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="dsi_ppai_maui.Views.HomeView"
             Title="IVR APP"
             xmlns:viewModel="clr-namespace:dsi_ppai_maui.ViewModels"
             x:DataType="viewModel:GestorConsulta"
             >
    <VerticalStackLayout>

        <Image HorizontalOptions="Center" VerticalOptions="Center" HeightRequest="200" Source="logo.png"/>

        <Button Text="Consultar encuesta" Command="{Binding ConsultarEncuestaCommand}" />

        
    </VerticalStackLayout>
</ContentPage>