<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="dsi_ppai_maui.Views.HomeView"
             Title="HomeView"
             xmlns:viewModel="clr-namespace:dsi_ppai_maui.ViewModels"
             x:DataType="viewModel:HomeViewModel"
             >
    <VerticalStackLayout>
        <Label 
            Text="Home"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

        <Button Text="Consultar encuesta" Command="{Binding GoToConsultarEncuestaCommand}" />

    </VerticalStackLayout>
</ContentPage>